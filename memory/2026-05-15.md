
## 02:21 — Перенос vault на Google Drive (остановлено)

### Статус: ПРИОСТАНОВЛЕНО до утра

### Что сделано:
- ✅ Получен Service Account JSON: `jarvis-2026-492009-8913a86ef9b7---24db52c6-ab3a-4254-9813-68ae401b2944.json`
- ✅ Ключ сохранён: `/root/.openclaw/secrets/gdrive_service_account.json`
- ✅ Конфиг rclone создан: `/root/.config/rclone/rclone.conf` (Service Account)
- ✅ Подключение к Google Drive работает (rclone lsd/ls видит папки)
- ✅ Тест записи/удаления прошёл (rclone touch/deletefile)

### Проблема:
- ❌ Загрузка файлов падает с ошибкой: `storageQuotaExceeded`
- Service Account не имеет собственного хранилища, файлы идут на твой Drive
- Вероятно, закончилось место на Google Drive (15G бесплатного плана)
- `rclone about gdrive:` показывает Used: 0B (не информативно для SA)

### Что нужно сделать утром:
1. **Проверить место на Google Drive** — drive.google.com → Настройки → Хранилище
2. Если место есть — повторить загрузку
3. Если места нет — либо почистить, либо использовать OAuth вместо Service Account
4. Альтернатива: купить Google One 100G (~150₽/мес)

### Следующие шаги после решения с квотой:
- Загрузить `/root/vault/` на `gdrive:Jarvis/vault/`
- Настроить автоматическую синхронизацию (cron)
- Обновить TOOLS.md и MEMORY.md с новым расположением vault

### Конфигурация:
```
[gdrive]
type = drive
service_account_file = /root/.openclaw/secrets/gdrive_service_account.json
scope = drive
```

### Файлы для загрузки (приоритет):
- `knowledge/viticulture/` — фенологическая шкала + фитоклимат
- `knowledge/` — все домены знаний
- `projects/` — проекты
- `docs/` — документация
- `ideas/` — идеи
- `learning-log.md` — лог обучения
